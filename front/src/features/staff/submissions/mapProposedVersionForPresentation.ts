/**
 * @deprecated Import from `@/features/discover/mapResourceVersionForPresentation`.
 * Kept so staff barrels continue to resolve during the unified-presentation refactor.
 */
export {
  mapProposedVersionForPresentation,
  mapResourceVersionForPresentation,
  parseNoteSections,
  type ResourceVersionPresentation,
  type ResourceVersionPresentation as ProposedVersionPresentation,
} from '@/features/discover/mapResourceVersionForPresentation'
