/**
 * Organization default password shown (read-only) when creating staff users.
 * Replace with config/API value when account provisioning is wired.
 */
export const DEFAULT_ORG_PASSWORD = 'WelcomeToRRCRC!'
